apply plugin: 'application'
mainClassName = "org.mrdlib.partnerContentManager.mediatum.MediaTUMDeletedDocumentScanner"
run {
    if(System.getProperty("exec.args") != null) {
        args System.getProperty("exec.args").split()
    } 
}